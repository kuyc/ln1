1. Цель архитектуры

Архитектура Playerok AI Seller Bot спроектирована так, чтобы:

исключить «забывание роли» агентами;

предотвратить удаление или поломку существующего функционала;

обеспечить масштабирование без переписывания ядра;

разделить логику, интеграции и AI.

Архитектура приоритетнее реализации.
Реализация не может нарушать архитектуру.

2. Архитектурные принципы (обязательные)

Single Source of Truth

/docs — единственный источник продуктовой и архитектурной правды.

Код обязан соответствовать документации.

Модульность

Каждый модуль имеет одну зону ответственности.

Модули не знают внутреннюю реализацию друг друга.

AI — сервис, не контроллер

AI не управляет потоками.

AI вызывается только из Workflow Engine.

Fail-safe по умолчанию

Ошибка → остановка автоматизации → уведомление администратора.

3. Высокоуровневая схема системы
[ Telegram Bot ]
       |
       v
[ Command / UI Layer ]
       |
       v
[ Workflow Engine ]  <─── Admin Confirmation
   |        |
   |        ├── Pricing Engine
   |        ├── AI Content Service
   |        ├── Playerok Integration
   |        └── Order Handler
       |
       v
[ Data Layer ]

4. Основные модули
4.1 Telegram Bot Layer

Ответственность:

приём команд;

кнопки (inline keyboard);

уведомления администратору;

статусы.

НЕ делает:

бизнес-логику;

вызовы AI напрямую;

работу с Playerok API.

4.2 Command / UI Layer

Ответственность:

интерпретация команд пользователя;

проверка прав (admin / user);

маршрутизация в Workflow Engine.

4.3 Workflow Engine (ЯДРО)

Ключевой модуль проекта.

Отвечает за:

последовательность действий;

различия между режимами (Semi / Auto);

контроль состояний;

точки подтверждения администратором.

Workflow Engine — единственный модуль, который:

знает бизнес-логику;

вызывает AI;

вызывает Playerok API;

принимает решения «что дальше».

5. Подсистемы Workflow Engine
5.1 AI Content Service

Функции:

генерация названия;

генерация описания;

генерация комментариев;

генерация ответов покупателю;

генерация превью.

Ограничения:

не знает режим работы;

не знает цену;

не публикует товары.

5.2 Pricing Engine

Функции:

расчёт стартовой цены;

анализ спроса;

корректировка цены со временем;

обучение на истории продаж.

Источники данных:

внутренняя база;

данные Playerok (парсинг).

5.3 Playerok Integration

Функции:

публикация товара;

изменение цены;

отслеживание заказов;

подтверждение заказа;

получение статусов.

Особенности:

работает через cookies;

реализует retry + rate limit;

не содержит бизнес-логики.

5.4 Order Handler

Функции:

реакция на покупку;

отправка сообщений покупателю;

подтверждение заказа;

передача данных в Pricing Engine.

6. Data Layer
Типы данных:

товары;

категории;

цены;

история продаж;

действия AI;

события Workflow.

Правила:

AI не пишет напрямую в БД;

все записи проходят через Workflow Engine;

изменения версионируются.

7. Режимы работы (архитектурно)
Semi-Auto

Workflow останавливается после публикации

Нет автоматического перевыставления

Auto

Workflow зациклен

После продажи → новая публикация

Цена и контент могут обновляться

👉 Подробно — docs/modes.md

8. Обработка ошибок

Любая ошибка:

фиксируется;

логируется;

автоматизация останавливается;

администратор уведомляется.

❌ Автоматический «продолжаем как-нибудь» запрещён.

9. Расширяемость

Разрешено:

добавлять новые категории;

добавлять новые AI-модели;

улучшать Pricing Engine.

Запрещено:

ломать существующие workflow;

менять роли модулей;

удалять подтверждения администратора.

10. Контроль соблюдения архитектуры

Любой PR проверяется:

Code Reviewer

Context Manager

Несоответствие архитектуре = отказ в мёрже.

11. Финальное правило

Если код противоречит архитектуре — код неверный.
Даже если он “работает”.
