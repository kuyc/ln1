1. Назначение документа

Этот документ строго описывает Workflow Engine:

порядок шагов;

точки принятия решений;

точки подтверждения администратора;

правила остановки и продолжения.

👉 Workflow = закон исполнения.
Никакая реализация не может от него отклоняться.

2. Определения
Workflow Engine

Центральный модуль системы, который:

управляет состояниями;

вызывает подсистемы (AI, Pricing, Playerok);

принимает решения на основе режима и состояния.

Шаг (Step)

Атомарное действие Workflow:

либо завершается успешно,

либо возвращает ошибку.

Состояние (State)

Текущее положение Workflow в жизненном цикле.

3. Общая модель Workflow
IDLE
 ↓
INIT
 ↓
DATA_COLLECTION
 ↓
CONTENT_GENERATION
 ↓
PREVIEW_GENERATION
 ↓
ADMIN_CONFIRMATION (опционально)
 ↓
PRICE_CALCULATION
 ↓
PUBLISH
 ↓
WAIT_FOR_SALE
 ↓
POST_SALE_ACTIONS
 ↓
STOP | LOOP (Auto)

4. Глобальные правила Workflow

Только один активный Workflow на товар

Каждый шаг логируется

Ошибка на любом шаге → ERROR_STOP

AI вызывается только внутри разрешённых шагов

Workflow Engine — единственный оркестратор

5. Подробное описание шагов
5.1 INIT

Цель: инициализация Workflow.

Действия:

проверка режима (Semi / Auto);

проверка прав администратора;

загрузка конфигурации (лимиты, категории).

Ошибка → ERROR_STOP

5.2 DATA_COLLECTION

Цель: сбор данных для генерации и цены.

Источники:

внутренняя БД;

данные Playerok (категория, конкуренты).

Выход:

структурированный dataset.

5.3 CONTENT_GENERATION

Цель: генерация текстового контента.

Вызывает:

AI Content Service.

Результат:

название;

описание;

комментарий.

❌ Не содержит цены
❌ Не публикует товар

5.4 PREVIEW_GENERATION

Цель: генерация превью товара.

Вызывает:

AI Image / Preview Generator.

Результат:

одно или несколько изображений.

5.5 ADMIN_CONFIRMATION

Обязателен, если:

режим = Semi-Auto;

режим = Auto и это первый запуск;

цена выходит близко к лимитам.

Администратор может:

подтвердить;

отклонить (→ STOP);

изменить параметры вручную.

5.6 PRICE_CALCULATION

Цель: определить итоговую цену.

Вызывает:

Pricing Engine.

Валидации:

цена в пределах MIN / MAX;

шаг изменения допустим;

причина изменения зафиксирована.

Ошибка → ADMIN_CONFIRMATION или ERROR_STOP.

5.7 PUBLISH

Цель: публикация товара.

Вызывает:

Playerok Integration.

Действия:

создание товара;

установка цены;

загрузка превью;

сохранение ID товара.

Ошибка → ERROR_STOP.

5.8 WAIT_FOR_SALE

Цель: ожидание события покупки.

Источники событий:

polling Playerok API;

webhook (если доступен).

Состояние:

пассивное ожидание;

таймеры для Auto режима.

5.9 POST_SALE_ACTIONS

Цель: обработка продажи.

Действия:

отправка шаблонных сообщений покупателю;

подтверждение заказа;

обновление статистики;

передача данных в Pricing Engine.

6. Поведение после продажи
Semi-Auto
POST_SALE_ACTIONS → STOP

Auto
POST_SALE_ACTIONS
 ↓
UPDATE_PRICE
 ↓
LOOP → INIT

7. ERROR_STOP (аварийное состояние)

При любом исключении:

Workflow останавливается;

состояние сохраняется;

администратор уведомляется;

автоматическое продолжение запрещено.

8. Управление Workflow
Администратор может:

запустить Workflow;

остановить Workflow;

подтвердить шаг;

изменить параметры.

Администратор НЕ может:

пропускать обязательные шаги;

игнорировать ошибки.

9. Запрещённые действия

AI не может:

запускать Workflow;

останавливать Workflow;

перескакивать шаги;

менять состояние напрямую.

Подсистемы не могут:

вызывать друг друга напрямую;

менять режим работы.

10. Логирование (обязательно)

Каждый шаг логирует:

workflow_id;

step_name;

статус;

входные параметры (без секретов);

результат или ошибку;

timestamp.

11. Финальное правило

Workflow — это конечный автомат.
Если шаг не описан — он не существует.
