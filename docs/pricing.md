1. Назначение документа

Этот документ описывает правила формирования, изменения и контроля цены товаров.

Он обязателен для:

Workflow Engine

Pricing Engine

AI (через ограничения)

Если правило не описано здесь — оно не применяется.

2. Принципы ценообразования (НЕ НАРУШАЮТСЯ)

Цена — бизнес-решение, не креатив

AI предлагает, но не решает.

Безопасность > прибыль

Цена не может выйти за допустимые пределы.

Предсказуемость

Изменения цены объяснимы и логируемы.

Контроль администратора

Все лимиты задаёт администратор.

3. Источники данных для цены
3.1 Внутренние данные (приоритет)

история продаж;

скорость продаж;

предыдущие цены;

успешность карточек.

3.2 Внешние данные

цены конкурентов на Playerok;

количество активных предложений;

позиции в выдаче (если доступны).

4. Стартовая цена

При создании товара стартовая цена определяется так:

ЕСЛИ есть данные по категории
 → использовать медиану успешных продаж
ИНАЧЕ
 → AI-анализ текущих предложений Playerok

Важно:

AI не может выходить за пределы лимитов;

итоговая цена всегда проходит валидацию Pricing Engine.

5. Ценообразование в Semi-Auto режиме
Разрешено:

расчёт стартовой цены;

отображение цены администратору;

ручное изменение цены администратором.

Запрещено:

автоматическое изменение цены;

динамическая корректировка;

обучение на данных без подтверждения.

📌 Цена фиксируется после публикации.

6. Ценообразование в Auto режиме
6.1 Разрешённые действия

автоматическое снижение цены;

автоматическое повышение цены;

адаптация под спрос;

обучение на результатах продаж.

6.2 Правила снижения цены

Цена может снижаться, если:

товар не продан за N часов;

конкуренты дешевле;

низкая конверсия просмотров → покупка.

Ограничения:

шаг снижения ≤ X%;

интервал между изменениями ≥ T минут;

цена ≥ MIN_PRICE.

6.3 Правила повышения цены

Цена может повышаться, если:

товар продаётся быстро;

спрос стабильно высокий;

цена ниже медианы рынка.

Ограничения:

шаг повышения ≤ Y%;

цена ≤ MAX_PRICE;

резкие скачки запрещены.

7. Лимиты цены (обязательные)

Задаются администратором:

MIN_PRICE — минимально допустимая цена;

MAX_PRICE — максимально допустимая цена;

MAX_STEP_UP — максимум повышения за шаг;

MAX_STEP_DOWN — максимум снижения за шаг.

AI и Pricing Engine не могут их менять.

8. Обучение и адаптация
Что можно:

учитывать успешные продажи;

учитывать скорость сделки;

учитывать цену конкурентов.

Что запрещено:

обучение без логирования;

использование внешних данных без валидации;

изменение логики обучения без обновления документации.

9. Логирование и аудит

Каждое изменение цены должно содержать:

старая цена;

новая цена;

причина изменения;

режим (Semi / Auto);

timestamp.

Отсутствие лога = ошибка.

10. Ошибки и аварийные ситуации

Если:

цена выходит за лимиты;

данные противоречивы;

расчёт невозможен;

➡️ Workflow Engine:

останавливает автоматизацию;

уведомляет администратора;

требует ручного решения.

11. Запрещённые действия (жёстко)

AI не может:

устанавливать лимиты;

игнорировать MIN/MAX;

менять цену в Semi-Auto;

изменять цену без причины.

12. Финальное правило

Цена — это система с ограничениями.
Любая «умная» логика без ограничений запрещена.
